<h1>Line Items Index</h1>
<p>(shows remaining items in this session's cart -- but we want to redirect back to the actual shopping cart)</p>

<table class="table">
  <thead>
    <tr>
      <th>Product</th>
      <th style="text-align: center;">Cart</th>
      <th style="text-align: center;">Quantity</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @line_items.each do |line_item| %>
      <tr>
        <td><%= line_item.product.name %></td>
        <td style="text-align: center;"><%= line_item.cart.id %></td>
        <td style="text-align: center;"><%= line_item.quantity %></td>
        <td><%= link_to 'Show', line_item %></td>
        <td><%= link_to 'Edit', edit_line_item_path(line_item) %></td>
        <td><%= link_to 'Destroy', line_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Line item', new_line_item_path %>
